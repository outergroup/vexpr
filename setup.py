from setuptools import find_packages, setup

setup(name="vexpr",
      version="0.0.1",
      description="Write your code as tree-like expressions, then transform it",
      author="Marcus Lewis",
      url="https://outerloop.xyz/",
      packages=find_packages(),
)
